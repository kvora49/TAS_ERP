Always read brain.md (at repo root) and module-specific brain.md files first when entering a session or task context. Use Serena MCP for fast, targeted symbol lookups (find_symbol, find_referencing_symbols) instead of doing open-ended codebase grep scans. Prioritize keeping the status and baseline metrics in these brain files current as components are refactored or migrated. Keep components small, modular, and accessible.